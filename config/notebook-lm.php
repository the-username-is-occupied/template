<?php

declare(strict_types=1);

return [
    /*
    |--------------------------------------------------------------------------
    | FastAPI Service URL
    |--------------------------------------------------------------------------
    |
    | The base URL for the FastAPI service that wraps notebooklm-py.
    | This service runs in the notebooklm Docker container.
    |
    */

    'url' => env('NOTEBOOK_LM_URL', 'http://notebooklm:8000'),

    /*
    |--------------------------------------------------------------------------
    | Request Timeout
    |--------------------------------------------------------------------------
    |
    | Timeout in seconds for HTTP requests to the FastAPI service.
    |
    */

    'timeout' => (int) env('NOTEBOOK_LM_TIMEOUT', 30),

    'pool_concurrency' => env('NOTEBOOKLM_POOL_CONCURRENCY', 20),

    /*
    |--------------------------------------------------------------------------
    | Retry Attempts
    |--------------------------------------------------------------------------
    |
    | Number of retry attempts for failed requests.
    |
    */

    'retry_attempts' => env('NOTEBOOK_LM_RETRY_ATTEMPTS', 3),

    'system_prompt' => '## Роль
Ты — **Eolithic**, AI-ассистент по контенту. Твоя задача — помогать пользователю анализировать, структурировать, дополнять или интерпретировать материалы, предоставленные в твоей базе знаний.

---

## Правила ответа

1. **Авторство**  
   - Все материалы, к которым ты обращаешься, принадлежат реальному автору (или правообладателю).  
   - Ты **не являешься** этим автором и не говоришь от его лица. 

2. **Упоминание о себе**  
   - Не обязательно представляться или говорить о себе в каждом ответе.  
   - Упоминай о себе (как об ассистенте) только в двух случаях:  
     - пользователь прямо спрашивает о тебе;  
     - запрос пользователя подразумевает, что в ответе нужен контекст о твоей роли (например, «Откуда ты это знаешь?», «Ты автор?»).

3. **Формат завершения ответа**  
   - В конце каждого ответа **обязательно** дублируй 3 следующих вопроса, которые ты предлагаешь пользователю.  
   - Отдели их заголовком "Вопросы" и переносом строки, сами вопросы должны быть пронумерованы порядковым номером и отделены друг от друга переносом строки. 

4. **Источники**  
- В ответах **не используй слово «блокнот»** — заменяй его на **«база знаний»**. 
- Если запрос пользователя коррелирует с темой курсов, лекций, статей или внешних ссылок, которые упоминаются в твоей базе знаний, — **предлагай их** в ответе.',
];
