<?php

declare(strict_types=1);

return [
    /*
    |--------------------------------------------------------------------------
    | FastAPI Service URL
    |--------------------------------------------------------------------------
    |
    | The base URL for the FastAPI service that wraps notebooklm-py.
    | This service runs in the notebooklm Docker container.
    |
    */

    'url' => env('NOTEBOOK_LM_URL', 'http://notebooklm:8000'),

    /*
    |--------------------------------------------------------------------------
    | Request Timeout
    |--------------------------------------------------------------------------
    |
    | Timeout in seconds for HTTP requests to the FastAPI service.
    |
    */

    'timeout' => (int) env('NOTEBOOK_LM_TIMEOUT', 30),

    'pool_concurrency' => env('NOTEBOOKLM_POOL_CONCURRENCY', 20),

    /*
    |--------------------------------------------------------------------------
    | Retry Attempts
    |--------------------------------------------------------------------------
    |
    | Number of retry attempts for failed requests.
    |
    */

    'retry_attempts' => env('NOTEBOOK_LM_RETRY_ATTEMPTS', 3),

    'system_prompt' => '## Роль
Ты — **Eolithic**, AI-ассистент по контенту.

---

## Правила ответа

1. **Упоминание о себе**  
   - Не обязательно представляться или говорить о себе в каждом ответе.  
   - Упоминай о себе (как об ассистенте) только в двух случаях:  
     - пользователь прямо спрашивает о тебе;  
     - запрос пользователя подразумевает, что в ответе нужен контекст о твоей роли (например, «Откуда ты это знаешь?», «Ты автор?»).

2. **Источники**  
- Если запрос пользователя коррелирует с темой курсов, лекций, статей или ссылок на контакты автора из источников, — аккуратно **предлагай их**. Учитывай актуальность по метатегам (published_at).
- Не упоминай источники, если они не релевантны запросу пользователя.
- Не предлагай действия из источников которые ты не умешь выполнять от своего имени',
];
